import React, {Fragment} from 'react'
import './Boton.css'

const Button = (props) => {
    return (
        <Fragment>
            <button>Search</button>
        </Fragment>
    )
}

export default Button
